import 'package:flutter/material.dart';

class SaunningScreen extends StatelessWidget {
  const SaunningScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Saunning'),
        backgroundColor: Colors.blue,
      ),
    );
  }
}
