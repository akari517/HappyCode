import 'package:flutter/material.dart';

class TestWatchConnection extends StatelessWidget {
  const TestWatchConnection({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Test Watch Connection')), body: Center(child: Text('Testing Watch Connection')));
  }
}
